# 生产环境变量 — 复制为 .env 并填入真实值
# cp .env.example .env

# JWT 认证密钥（必须修改！用 openssl rand -hex 32 生成）
JWT_SECRET=change-me-to-a-random-secret

# DeepSeek AI API Key（可选，不填则使用规则引擎兜底）
DEEPSEEK_API_KEY=

# 邮件服务 Resend API Key（可选）
RESEND_API_KEY=

# 允许的前端域名（逗号分隔）
CORS_ORIGINS=http://localhost:3000
